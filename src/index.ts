console.log('🏈 Soccer Session Manager - API');
console.log('✅ TypeScript configurado com sucesso!');

const testFunction = (name: string): string => {
  return `Hello, ${name}!`;
};

console.log(testFunction('Futebol'));
